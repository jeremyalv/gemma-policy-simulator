# Simulation Engine Guidance

- One persona equals one isolated prompt context.
- Enforce strict JSON output parsing and typed validation.
- Record prompt template and model versions for every run.
- Retries must be bounded and explicitly logged.
