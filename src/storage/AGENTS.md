# Storage Guidance

- Store simulation metadata required for reproducibility.
- Keep raw outputs append-only for auditability.
- Export formats must preserve `simulation_id` and `persona_id` linkage.
