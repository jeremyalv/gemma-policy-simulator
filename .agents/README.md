# .agents

This directory holds Codex project memory that is reusable across sessions.

- `skills/`: reusable expert workflows.
- `guardrails/`: paths and constraints that require extra care.

Use these by referencing the skill intent in your request, for example: "use the code review skill".
